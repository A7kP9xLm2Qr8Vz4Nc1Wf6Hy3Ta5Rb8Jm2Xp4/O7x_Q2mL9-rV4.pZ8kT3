local a = game:GetService("TweenService")
local b = game:GetService("UserInputService")
local c = game:GetService("CoreGui")
local d = game:GetService("RunService")
local e = {
	background = Color3.fromRGB(9, 5, 5),
	headerBackground = Color3.fromRGB(6, 3, 3),
	sidebarBackground = Color3.fromRGB(5, 2, 2),
	buttonBackground = Color3.fromRGB(24, 10, 10),
	inputBackground = Color3.fromRGB(16, 7, 7),
	accentStroke = Color3.fromRGB(255, 35, 35),
	accentFill = Color3.fromRGB(255, 35, 35),
	accentSelected = Color3.fromRGB(75, 12, 12),
	textPrimary = Color3.fromRGB(255, 255, 255),
	textSecondary = Color3.fromRGB(255, 155, 155),
	textMuted = Color3.fromRGB(255, 200, 200),
	divider = Color3.fromRGB(55, 20, 20),
	font = Enum.Font.PermanentMarker,
	fontBody = Enum.Font.PermanentMarker
}
local f = 10
local g = 7
local h = 420
local i = 300
local j = 35
local k = 50
local l = 22
local m = 0.55
local n = "▾"
local o = "▸"
local function p(ba, bb)
	local bc = Instance.new(ba)
	for bd, be in pairs(bb or {}) do
		bc[bd] = be
	end
	return bc
end
local function q(bf, bg)
	return p("UICorner", {
		Parent = bf,
		CornerRadius = UDim.new(0, bg or g)
	})
end
local function r(bh, bi, bj)
	return p("UIStroke", {
		Parent = bh,
		Color = bi or e.accentStroke,
		Thickness = bj or 1.5
	})
end
local function s(bk)
	bk = math.max(0, math.floor(bk))
	local bl = math.floor(bk / 86400)
	local bm = math.floor((bk % 86400) / 3600)
	local bn = math.floor((bk % 3600) / 60)
	local bo = bk % 60
	return string.format("%dD / %dH / %dM / %dS", bl, bm, bn, bo)
end
local t = {}
t.__index = t
function t.new(bp)
	bp = bp or {}
	local bq = setmetatable({}, t)
	bq.tabs = {}
	bq.tabOffsets = {}
	bq.selectedIndex = 1
	bq.dropdowns = {}
	local br = p("ScreenGui", {
		Name = bp.Name or "e",
		ResetOnSpawn = false,
		Parent = c
	})
	bq.screenGui = br
	local bs = p("TextButton", {
		Parent = br,
		Size = UDim2.new(0, 45, 0, 45),
		Position = UDim2.new(0.05, 0, 0.1, 0),
		BackgroundColor3 = e.headerBackground,
		Text = "",
		AutoButtonColor = false
	})
	q(bs, 12)
	p("UIGradient", {
		Parent = bs,
		Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 70, 70)),
			ColorSequenceKeypoint.new(0.45, Color3.fromRGB(255, 30, 30)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(140, 10, 10))
		}),
		Rotation = 45
	})
	local bt = r(bs)
	p("UIGradient", {
		Parent = bt,
		Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 120, 120)),
			ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 40, 40)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(160, 15, 15))
		}),
		Rotation = 45
	})
	p("TextLabel", {
		Parent = bs,
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundTransparency = 1,
		Text = bp.ToggleLabel or "T",
		Font = e.font,
		TextSize = 26,
		TextColor3 = e.textPrimary
	})
	local bu = false
	local bv
	local bw
	local bx = false
	bs.InputBegan:Connect(function(by)
		if by.UserInputType == Enum.UserInputType.MouseButton1
			or by.UserInputType == Enum.UserInputType.Touch then
			bu = true
			bx = false
			bv = by.Position
			bw = bs.Position
		end
	end)
	bs.InputEnded:Connect(function(bz)
		if bz.UserInputType == Enum.UserInputType.MouseButton1
			or bz.UserInputType == Enum.UserInputType.Touch then
			bu = false
		end
	end)
	local ca = bp.Width or 680
	local cb = bp.Height or 460
	local cc = p("Frame", {
		Parent = br,
		Size = UDim2.new(0, ca, 0, cb),
		Position = UDim2.new(0.5, -ca / 2, 0.5, -cb / 2),
		BackgroundColor3 = e.background,
		BorderSizePixel = 0,
		Active = true,
		ClipsDescendants = true,
		Visible = true
	})
	q(cc, f)
	r(cc)
	bq.panel = cc
	local cd = p("Frame", {
		Parent = cc,
		Size = UDim2.new(1, 0, 0, j),
		Position = UDim2.new(0, 0, 0, 0),
		BackgroundColor3 = e.headerBackground
	})
	q(cd, f)
	p("TextLabel", {
		Parent = cd,
		Size = UDim2.new(1, -10, 1, 0),
		Position = UDim2.new(0, 8, 0, 0),
		BackgroundTransparency = 1,
		Text = bp.Title or "e",
		TextColor3 = e.textPrimary,
		Font = e.font,
		TextSize = 14,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	local ce = p("Frame", {
		Parent = cc,
		Size = UDim2.new(0, 140, 1, -(j + k + l)),
		Position = UDim2.new(0, 0, 0, j),
		BackgroundColor3 = e.sidebarBackground
	})
	local cf = p("Frame", {
		Parent = ce,
		Size = UDim2.new(0, 4, 0, 32),
		Position = UDim2.new(0, 5, 0, 10),
		BackgroundColor3 = e.accentFill
	})
	q(cf, 2)
	bq.indicator = cf
	local cg = p("ScrollingFrame", {
		Parent = ce,
		Size = UDim2.new(1, -5, 1, 0),
		BackgroundTransparency = 1,
		ScrollBarThickness = 0,
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		CanvasSize = UDim2.new(0, 0, 0, 0)
	})
	p("UIListLayout", { Parent = cg, Padding = UDim.new(0, 5) })
	p("UIPadding", { Parent = cg, PaddingTop = UDim.new(0, 10) })
	bq.tabList = cg
	local ch = p("Frame", {
		Parent = cc,
		Size = UDim2.new(1, -145, 1, -(j + k + l)),
		Position = UDim2.new(0, 145, 0, j),
		BackgroundTransparency = 1
	})
	bq.content = ch
	local ci = p("Frame", {
		Parent = cc,
		Size = UDim2.new(1, 0, 0, k),
		Position = UDim2.new(0, 0, 1, -(k + l)),
		BackgroundColor3 = e.headerBackground
	})
	local cj = p("ImageLabel", {
		Parent = ci,
		Size = UDim2.new(0, 34, 0, 34),
		Position = UDim2.new(0, 8, 0.5, -17),
		BackgroundColor3 = e.inputBackground,
		Image = bp.AvatarImage or ""
	})
	q(cj, 17)
	r(cj, e.accentStroke, 1)
	p("TextLabel", {
		Parent = ci,
		Size = UDim2.new(1, -58, 1, 0),
		Position = UDim2.new(0, 50, 0, 0),
		BackgroundTransparency = 1,
		Text = bp.FooterText or "",
		TextColor3 = e.textPrimary,
		Font = e.font,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	local ck = p("Frame", {
		Parent = cc,
		Size = UDim2.new(1, 0, 0, l),
		Position = UDim2.new(0, 0, 1, -l),
		BackgroundColor3 = e.accentSelected
	})
	q(ck, f)
	local cl = p("TextLabel", {
		Parent = ck,
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundTransparency = 1,
		Text = "",
		TextColor3 = e.textPrimary,
		Font = e.fontBody,
		TextSize = 12
	})
	bq.timerLabel = cl
	local cm = os.clock()
	function bq:ResetTimer()
		cm = os.clock()
	end
	cl.Text = s(0)
	local cn = 0
	d.Heartbeat:Connect(function(co)
		cn += co
		if cn >= 1 then
			cn -= 1
			cl.Text = s(os.clock() - cm)
		end
	end)
	local cp = p("TextButton", {
		Parent = cc,
		Size = UDim2.new(0, 18, 0, 18),
		Position = UDim2.new(1, -18, 1, -18),
		BackgroundTransparency = 1,
		AutoButtonColor = false,
		Text = "",
		ZIndex = 10
	})
	local cq = true
	bs.MouseButton1Click:Connect(function()
		if bx then return end
		cq = not cq
		cc.Visible = cq
	end)
	bq.Open = function()
		cq = true
		cc.Visible = true
	end
	bq.Close = function()
		cq = false
		cc.Visible = false
	end
	local cr, cs, ct
	local cu, cv, cw
	local function cx(cy)
		if cy.UserInputType == Enum.UserInputType.MouseButton1
			or cy.UserInputType == Enum.UserInputType.Touch then
			cr = true
			cs = cy.Position
			ct = cc.Position
		end
	end
	local function cz(da)
		if da.UserInputType == Enum.UserInputType.MouseButton1
			or da.UserInputType == Enum.UserInputType.Touch then
			cr = false
		end
	end
	cd.InputBegan:Connect(cx)
	cd.InputEnded:Connect(cz)
	ce.InputBegan:Connect(cx)
	ce.InputEnded:Connect(cz)
	cg.InputBegan:Connect(cx)
	cg.InputEnded:Connect(cz)
	ch.InputBegan:Connect(cx)
	ch.InputEnded:Connect(cz)
	ci.InputBegan:Connect(cx)
	ci.InputEnded:Connect(cz)
	ck.InputBegan:Connect(cx)
	ck.InputEnded:Connect(cz)
	cp.InputBegan:Connect(function(db)
		if db.UserInputType == Enum.UserInputType.MouseButton1
			or db.UserInputType == Enum.UserInputType.Touch then
			cu = true
			cv = db.Position
			cw = cc.Size
		end
	end)
	cp.InputEnded:Connect(function(dc)
		if dc.UserInputType == Enum.UserInputType.MouseButton1
			or dc.UserInputType == Enum.UserInputType.Touch then
			cu = false
		end
	end)
	b.InputChanged:Connect(function(dd)
		if dd.UserInputType ~= Enum.UserInputType.MouseMovement
			and dd.UserInputType ~= Enum.UserInputType.Touch then
			return
		end
		if cr then
			local de = dd.Position - cs
			cc.Position = UDim2.new(
				ct.X.Scale, ct.X.Offset + de.X,
				ct.Y.Scale, ct.Y.Offset + de.Y
			)
		elseif cu then
			local df = dd.Position - cv
			local dg = math.max(h, cw.X.Offset + df.X)
			local dh = math.max(i, cw.Y.Offset + df.Y)
			cc.Size = UDim2.new(cw.X.Scale, dg, cw.Y.Scale, dh)
		elseif bu then
			local di = dd.Position - bv
			if math.abs(di.X) > 3 or math.abs(di.Y) > 3 then
				bx = true
			end
			bs.Position = UDim2.new(
				bw.X.Scale, bw.X.Offset + di.X,
				bw.Y.Scale, bw.Y.Offset + di.Y
			)
		end
	end)
	bq._cx = cx
	bq._cz = cz
	return bq
end
local di = {}
di.__index = di
function t:AddTab(dj)
	local dk = #self.tabs + 1
	local dl = p("TextButton", {
		Parent = self.tabList,
		Size = UDim2.new(1, -15, 0, 32),
		BackgroundColor3 = e.buttonBackground,
		BackgroundTransparency = 1,
		Text = "   " .. dj,
		TextColor3 = e.textSecondary,
		Font = e.font,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	q(dl)
	dl.MouseEnter:Connect(function()
		if dl.BackgroundTransparency == 1 then
			a:Create(dl, TweenInfo.new(0.15), {
				BackgroundTransparency = 0,
				BackgroundColor3 = Color3.fromRGB(45, 12, 12)
			}):Play()
		end
	end)
	dl.MouseLeave:Connect(function()
		if dl.TextColor3 ~= e.textPrimary then
			a:Create(dl, TweenInfo.new(0.15), {
				BackgroundTransparency = 1
			}):Play()
		end
	end)
	local dm = p("Frame", {
		Parent = self.content,
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundTransparency = 1,
		Visible = dk == 1
	})
	local dn = p("ScrollingFrame", {
		Parent = dm,
		Size = UDim2.new(m, -4, 1, 0),
		Position = UDim2.new(0, 0, 0, 0),
		BackgroundTransparency = 1,
		ScrollBarThickness = 4,
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		CanvasSize = UDim2.new(0, 0, 0, 0)
	})
	p("UIListLayout", { Parent = dn, Padding = UDim.new(0, 8) })
	p("UIPadding", {
		Parent = dn,
		PaddingTop = UDim.new(0, 8),
		PaddingLeft = UDim.new(0, 8),
		PaddingRight = UDim.new(0, 8)
	})
	p("Frame", {
		Parent = dm,
		Size = UDim2.new(0, 1, 1, -12),
		Position = UDim2.new(m, 0, 0, 6),
		BackgroundColor3 = e.divider,
		BorderSizePixel = 0
	})
	local do_ = p("ScrollingFrame", {
		Parent = dm,
		Size = UDim2.new(1 - m, -4, 1, 0),
		Position = UDim2.new(m, 4, 0, 0),
		BackgroundTransparency = 1,
		ScrollBarThickness = 4,
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		CanvasSize = UDim2.new(0, 0, 0, 0)
	})
	p("UIListLayout", { Parent = do_, Padding = UDim.new(0, 8) })
	p("UIPadding", {
		Parent = do_,
		PaddingTop = UDim.new(0, 8),
		PaddingLeft = UDim.new(0, 8),
		PaddingRight = UDim.new(0, 8)
	})
	if self._cx and self._cz then
		dn.InputBegan:Connect(self._cx)
		dn.InputEnded:Connect(self._cz)
		do_.InputBegan:Connect(self._cx)
		do_.InputEnded:Connect(self._cz)
		dm.InputBegan:Connect(self._cx)
		dm.InputEnded:Connect(self._cz)
	end
	table.insert(self.tabs, { Button = dl, Page = dm })
	table.insert(self.tabOffsets, 10 + (dk - 1) * 37)
	dl.MouseButton1Click:Connect(function()
		self:SelectTab(dk)
	end)
	return setmetatable({
		page = dm,
		main = dn,
		side = do_,
		library = self
	}, di)
end
function t:SelectTab(dp)
	self.selectedIndex = dp
	for dq, dr in ipairs(self.tabs) do
		dr.Page.Visible = (dq == dp)
		if dq == dp then
			a:Create(dr.Button, TweenInfo.new(0.15), {
				BackgroundTransparency = 0,
				BackgroundColor3 = e.accentSelected,
				TextColor3 = e.textPrimary
			}):Play()
		else
			a:Create(dr.Button, TweenInfo.new(0.15), {
				BackgroundTransparency = 1,
				TextColor3 = e.textSecondary
			}):Play()
		end
	end
	a:Create(self.indicator, TweenInfo.new(0.2), {
		Position = UDim2.new(0, 5, 0, self.tabOffsets[dp] or 10)
	}):Play()
end
function di:AddSectionLabel(ds, dt)
	local du = dt and self.side or self.main
	return p("TextLabel", {
		Parent = du,
		Size = UDim2.new(1, 0, 0, 22),
		BackgroundTransparency = 1,
		Text = ds,
		TextColor3 = e.accentFill,
		Font = e.fontBody,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
end
function di:AddButton(dv, dw, dx)
	local dy = dx and self.side or self.main
	local dz = p("TextButton", {
		Parent = dy,
		Size = UDim2.new(1, 0, 0, 36),
		BackgroundColor3 = e.buttonBackground,
		Text = "   " .. dv,
		TextColor3 = e.textMuted,
		Font = e.fontBody,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	q(dz)
	r(dz, Color3.fromRGB(90, 25, 25), 1)
	dz.Activated:Connect(function()
		if dw then dw() end
	end)
	return dz
end
function di:AddToggle(ea, eb, ec)
	local ed = ec and self.side or self.main
	local ee = p("TextButton", {
		Parent = ed,
		Size = UDim2.new(1, 0, 0, 36),
		BackgroundColor3 = e.buttonBackground,
		Text = "   " .. ea,
		TextColor3 = e.textMuted,
		Font = e.fontBody,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	q(ee)
	local ef = p("Frame", {
		Parent = ee,
		Size = UDim2.new(0, 16, 0, 16),
		Position = UDim2.new(1, -28, 0.5, -8),
		BackgroundColor3 = e.inputBackground
	})
	q(ef, 4)
	local eg = p("TextLabel", {
		Parent = ef,
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundTransparency = 1,
		Text = "",
		TextColor3 = e.textPrimary,
		Font = e.font,
		TextSize = 12
	})
	local eh = false
	ee.Activated:Connect(function()
		eh = not eh
		eg.Text = eh and "" or ""
		ef.BackgroundColor3 = eh and e.accentFill or e.inputBackground
		if eb then eb(eh) end
	end)
	return ee
end
function di:AddTextbox(ei, ej, ek, el)
	local em = el and self.side or self.main
	local en = p("Frame", {
		Parent = em,
		Size = UDim2.new(1, 0, 0, 36),
		BackgroundColor3 = e.buttonBackground
	})
	q(en)
	r(en, Color3.fromRGB(90, 25, 25), 1)
	p("TextLabel", {
		Parent = en,
		Size = UDim2.new(0.45, 0, 1, 0),
		Position = UDim2.new(0, 10, 0, 0),
		BackgroundTransparency = 1,
		Text = ei,
		TextColor3 = e.textMuted,
		Font = e.fontBody,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	local eo = p("TextBox", {
		Parent = en,
		Size = UDim2.new(0.5, -14, 0, 24),
		Position = UDim2.new(0.5, 0, 0.5, -12),
		BackgroundColor3 = e.inputBackground,
		TextColor3 = e.textPrimary,
		PlaceholderText = ej or "",
		PlaceholderColor3 = e.textSecondary,
		Font = e.fontBody,
		TextSize = 12,
		Text = "",
		ClearTextOnFocus = false
	})
	q(eo, 5)
	eo.FocusLost:Connect(function(ep)
		if ek then ek(eo.Text, ep) end
	end)
	return eo
end
function di:AddSlider(eq, er, es, et, eu, ev)
	local ew = ev and self.side or self.main
	er = er or 0
	es = es or 100
	et = math.clamp(et or er, er, es)
	local ex = p("Frame", {
		Parent = ew,
		Size = UDim2.new(1, 0, 0, 52),
		BackgroundColor3 = e.buttonBackground
	})
	q(ex)
	r(ex, Color3.fromRGB(90, 25, 25), 1)
	p("TextLabel", {
		Parent = ex,
		Size = UDim2.new(1, -70, 0, 20),
		Position = UDim2.new(0, 10, 0, 4),
		BackgroundTransparency = 1,
		Text = eq,
		TextColor3 = e.textMuted,
		Font = e.fontBody,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left
	})
	local ey = p("TextBox", {
		Parent = ex,
		Size = UDim2.new(0, 50, 0, 20),
		Position = UDim2.new(1, -60, 0, 4),
		BackgroundColor3 = e.inputBackground,
		TextColor3 = e.textPrimary,
		Font = e.fontBody,
		TextSize = 12,
		Text = tostring(et),
		ClearTextOnFocus = false
	})
	q(ey, 5)
	local ez = p("Frame", {
		Parent = ex,
		Size = UDim2.new(1, -20, 0, 6),
		Position = UDim2.new(0, 10, 1, -14),
		BackgroundColor3 = e.inputBackground
	})
	q(ez, 3)
	local fa = p("Frame", {
		Parent = ez,
		Size = UDim2.new(0, 0, 1, 0),
		BackgroundColor3 = e.accentFill
	})
	q(fa, 3)
	local fb = p("Frame", {
		Parent = ez,
		Size = UDim2.new(0, 12, 0, 12),
		Position = UDim2.new(0, -6, 0.5, -6),
		BackgroundColor3 = e.textPrimary,
		ZIndex = 5
	})
	q(fb, 6)
	local fc = et
	local function fd(fe, ff)
		fe = math.clamp(fe, er, es)
		fc = fe
		local fg = (es - er) == 0 and 0 or (fe - er) / (es - er)
		fa.Size = UDim2.new(fg, 0, 1, 0)
		fb.Position = UDim2.new(fg, -6, 0.5, -6)
		if not ff then
			ey.Text = tostring(math.floor(fe * 100) / 100)
		end
		if eu then eu(fe) end
	end
	fd(et)
	local fh = false
	local function fi(fj)
		local fk = (fj - ez.AbsolutePosition.X) / ez.AbsoluteSize.X
		fd(er + fk * (es - er))
	end
	ez.InputBegan:Connect(function(fl)
		if fl.UserInputType == Enum.UserInputType.MouseButton1
			or fl.UserInputType == Enum.UserInputType.Touch then
			fh = true
			fi(fl.Position.X)
		end
	end)
	ez.InputEnded:Connect(function(fm)
		if fm.UserInputType == Enum.UserInputType.MouseButton1
			or fm.UserInputType == Enum.UserInputType.Touch then
			fh = false
		end
	end)
	b.InputChanged:Connect(function(fn)
		if fh and (fn.UserInputType == Enum.UserInputType.MouseMovement
			or fn.UserInputType == Enum.UserInputType.Touch) then
			fi(fn.Position.X)
		end
	end)
	b.InputEnded:Connect(function(fo)
		if fo.UserInputType == Enum.UserInputType.MouseButton1
			or fo.UserInputType == Enum.UserInputType.Touch then
			fh = false
		end
	end)
	ey.FocusLost:Connect(function()
		local fp = tonumber(ey.Text)
		if fp then
			fd(fp, true)
			ey.Text = tostring(fc)
		else
			ey.Text = tostring(fc)
		end
	end)
	return {
		Set = fd,
		Get = function() return fc end
	}
end
function di:AddDropdown(fq, fr, fs, ft)
	if fs == nil then fs = true end
	local fu = fs and self.side or self.main
	local fv = self.library
	local fw = 24
	local fx = 2
	local fy = 150
	local fz = p("Frame", {
		Parent = fu,
		Size = UDim2.new(1, 0, 0, 26),
		BackgroundTransparency = 1,
		AutomaticSize = Enum.AutomaticSize.Y,
		ZIndex = 2
	})
	p("UIListLayout", { Parent = fz, Padding = UDim.new(0, 4) })
	local ga = p("TextButton", {
		Parent = fz,
		Size = UDim2.new(1, 0, 0, 26),
		BackgroundColor3 = e.buttonBackground,
		Text = "",
		AutoButtonColor = false,
		ZIndex = 2
	})
	q(ga)
	local gb = p("TextLabel", {
		Parent = ga,
		Size = UDim2.new(1, -10, 1, 0),
		Position = UDim2.new(0, 10, 0, 0),
		BackgroundTransparency = 1,
		Text = fq .. "  " .. n,
		TextColor3 = e.accentFill,
		Font = e.fontBody,
		TextSize = 13,
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 2
	})
	local gc = p("Frame", {
		Parent = fz,
		Size = UDim2.new(1, 0, 0, 20),
		BackgroundTransparency = 1,
		ZIndex = 2
	})
	local gd = p("TextButton", {
		Parent = gc,
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundColor3 = e.inputBackground,
		Text = "Close  ×",
		TextColor3 = e.textSecondary,
		Font = e.fontBody,
		TextSize = 12,
		ZIndex = 2
	})
	q(gd, 4)
	local ge = p("ScrollingFrame", {
		Parent = fz,
		Size = UDim2.new(1, 0, 0, 0),
		CanvasSize = UDim2.new(0, 0, 0, 0),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		ScrollBarThickness = 4,
		BackgroundColor3 = e.inputBackground,
		ClipsDescendants = true,
		Visible = true,
		ZIndex = 2
	})
	q(ge, 6)
	p("UIListLayout", { Parent = ge, Padding = UDim.new(0, fx) })
	p("UIPadding", {
		Parent = ge,
		PaddingTop = UDim.new(0, 4),
		PaddingBottom = UDim.new(0, 4),
		PaddingLeft = UDim.new(0, 4),
		PaddingRight = UDim.new(0, 4)
	})
	local gf = {
		frame = fz,
		content = ge,
		multiSelect = ft and true or false,
		selected = {},
		_options = {}
	}
	local gg = true
	local function gh()
		local gi = #gf._options
		local gj = 0
		if gi > 0 then
			gj = math.min(gi * fw + (gi - 1) * fx + 8, fy)
		end
		ge.Size = UDim2.new(1, 0, 0, gj)
	end
	local function gk(gl)
		fz.ZIndex = gl
		ga.ZIndex = gl
		gb.ZIndex = gl
		gc.ZIndex = gl
		gd.ZIndex = gl
		ge.ZIndex = gl
		for _, gm in ipairs(ge:GetChildren()) do
			if gm:IsA("GuiObject") then
				gm.ZIndex = gl
			end
		end
	end
	function gf:SetOpen(gn)
		gg = gn
		ge.Visible = gn
		gc.Visible = gn
		gb.Text = fq .. "  " .. (gn and n or o)
		gk(gn and 10 or 2)
		if gn and fv then
			for _, go in ipairs(fv.dropdowns) do
				if go ~= gf and go._IsExpanded and go._IsExpanded() then
					go:SetOpen(false)
				end
			end
		end
	end
	gf._IsExpanded = function()
		return gg
	end
	function gf:AddOption(gp, gq)
		local gr = p("TextButton", {
			Parent = ge,
			Size = UDim2.new(1, 0, 0, fw),
			BackgroundTransparency = 1,
			Text = "  " .. gp,
			TextColor3 = Color3.fromRGB(255, 170, 170),
			Font = Enum.Font.Gotham,
			TextSize = 12,
			TextXAlignment = Enum.TextXAlignment.Left,
			ZIndex = gg and 10 or 2,
			AutoButtonColor = false
		})
		local gs = false
		local gt
		local function gu()
			if gs then
				gr.BackgroundTransparency = 0
				gr.BackgroundColor3 = e.accentSelected
				gr.TextColor3 = e.textPrimary
				gr.Text = (gf.multiSelect and "✓ " or "  ") .. gp
			else
				gr.BackgroundTransparency = 1
				gr.TextColor3 = Color3.fromRGB(255, 170, 170)
				gr.Text = "  " .. gp
			end
		end
		local function gv(gw)
			gs = gw
			gu()
			gf.selected[gp] = gw or nil
		end
		gr.MouseEnter:Connect(function()
			if not gs then
				gr.BackgroundTransparency = 0
				gr.BackgroundColor3 = Color3.fromRGB(45, 12, 12)
			end
		end)
		gr.MouseLeave:Connect(function()
			if not gs then
				gr.BackgroundTransparency = 1
			end
		end)
		gr.Activated:Connect(function()
			if gf.multiSelect then
				gv(not gs)
				if gq then gq(gs) end
			else
				for _, gx in ipairs(gf._options) do
					if gx ~= gt then
						gx.SetSelected(false)
					end
				end
				gv(true)
				if gq then gq() end
			end
		end)
		gt = { Button = gr, Text = gp, SetSelected = gv }
		table.insert(gf._options, gt)
		gh()
		return gr
	end
	function gf:RemoveOption(gy)
		if not gy then return end
		for gz, ha in ipairs(gf._options) do
			if ha.Button == gy then
				gf.selected[ha.Text] = nil
				table.remove(gf._options, gz)
				break
			end
		end
		gy:Destroy()
		gh()
	end
	function gf:Clear()
		for _, hb in ipairs(ge:GetChildren()) do
			if hb:IsA("TextButton") then
				hb:Destroy()
			end
		end
		gf._options = {}
		gf.selected = {}
		gh()
	end
	function gf:GetSelected()
		local hc = {}
		for _, hd in ipairs(gf._options) do
			if gf.selected[hd.Text] then
				table.insert(hc, hd.Text)
			end
		end
		return hc
	end
	ga.Activated:Connect(function()
		gf:SetOpen(not gg)
	end)
	gd.Activated:Connect(function()
		gf:SetOpen(false)
	end)
	if fr then
		for _, he in ipairs(fr) do
			gf:AddOption(he.Text, he.Callback)
		end
	end
	if fv then
		table.insert(fv.dropdowns, gf)
	end
	gf:SetOpen(true)
	return gf
end
return t
