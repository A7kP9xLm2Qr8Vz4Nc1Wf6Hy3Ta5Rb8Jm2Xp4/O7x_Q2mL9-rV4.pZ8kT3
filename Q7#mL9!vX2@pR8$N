local a = game:GetService("Players")
local b = game:GetService("RunService")
local c = game:GetService("Lighting")
local d = game:GetService("ReplicatedStorage")
local e = loadstring(game:HttpGet("https://raw.githubusercontent.com/A7kP9xLm2Qr8Vz4Nc1Wf6Hy3Ta5Rb8Jm2Xp4/O7x_Q2mL9-rV4.pZ8kT3/refs/heads/main/N7%23qL9%40vX2!mRp8%24"))()
local f = a.LocalPlayer
local g = f.DisplayName
local h = workspace.CurrentCamera
_G.blacklistedPlayers = _G.blacklistedPlayers or {}
_G.tpListedPlayers = _G.tpListedPlayers or {}
_G.watchlistedPlayers = _G.watchlistedPlayers or {}
local function i(plr)
	for _, name in ipairs(_G.blacklistedPlayers) do
		if name:lower() == plr.Name:lower() then return true end
	end
	return false
end
local function j(plr)
	for _, name in ipairs(_G.tpListedPlayers) do
		if name:lower() == plr.Name:lower() then return true end
	end
	return false
end
local function k(plr)
	local n = plr.Name:lower()
	local d = plr.DisplayName:lower()
	for _, word in ipairs(_G.watchlistedPlayers) do
		local w = word:lower()
		if n:find(w, 1, true) or d:find(w, 1, true) then
			return true
		end
	end
	return false
end
local function l()
	if not f.Character then
		repeat task.wait() until f.Character
	end
	return f.Character
end
local function m(plr)
	return plr and plr.Character
		and plr.Character:FindFirstChild("HumanoidRootPart")
		and plr.Character:FindFirstChild("Humanoid")
		and plr.Character.Humanoid.Health > 0
end
local function n()
	for _, tool in pairs(f.Backpack:GetChildren()) do
		if tool.Name == "Punch" and f.Character and f.Character:FindFirstChild("Humanoid") then
			f.Character.Humanoid:EquipTool(tool)
		end
	end
	if f:FindFirstChild("muscleEvent") then
		f.muscleEvent:FireServer("punch", "leftHand")
		f.muscleEvent:FireServer("punch", "rightHand")
	end
end
local function o(plr)
	if not m(plr) or not l():FindFirstChild("LeftHand") then return end
	pcall(function()
		firetouchinterest(plr.Character.HumanoidRootPart, l().LeftHand, 0)
		firetouchinterest(plr.Character.HumanoidRootPart, l().LeftHand, 1)
		n()
	end)
end
local function p(n)
	if n >= 1e15 then return string.format("%.1fqa", n / 1e15)
	elseif n >= 1e12 then return string.format("%.1ft", n / 1e12)
	elseif n >= 1e9  then return string.format("%.1fb", n / 1e9)
	elseif n >= 1e6  then return string.format("%.1fm", n / 1e6)
	elseif n >= 1e3  then return string.format("%.1fk", n / 1e3)
	else return tostring(n) end
end
local function q(n)
	local s = tostring(math.floor(n))
	local k
	while true do
		s, k = s:gsub("^(-?%d+)(%d%d%d)", "%1,%2")
		if k == 0 then break end
	end
	return s
end
local r = e.new({
	Title = g .. " Is Smol",
	FooterText = "Credits: Tree",
	AvatarImage = "rbxthumb://type=AvatarHeadShot&id=" .. f.UserId .. "&w=150&h=150",
	Width = 600,
	Height = 400
})
local s = r:AddTab("Misc")
local t = r:AddTab("Killing")
local u = r:AddTab("Teleporting")
local v = r:AddTab("Spectating")
local w = r:AddTab("Pets")
local x = r:AddTab("Stats")
local function y(tab)
	for _, child in ipairs(tab.page:GetChildren()) do
		if child:IsA("Frame") and child.Size.X.Offset == 1 then
			child:Destroy()
		end
	end
end
y(s)
y(v)
y(w)
s:AddButton("Anti Lag", function()
	local terrain = workspace:FindFirstChildWhichIsA("Terrain")
	if terrain then
		terrain.WaterWaveSize = 0
		terrain.WaterWaveSpeed = 0
		terrain.WaterReflectance = 0
		terrain.WaterTransparency = 1
	end
	c.ClockTime = 0
	c.GlobalShadows = false
	c.FogEnd = 9e9
	c.FogStart = 9e9
	settings().Rendering.QualityLevel = 1
	for _, v in pairs(game:GetDescendants()) do
		if v:IsA("BasePart") then
			v.CastShadow  = false
			v.Material    = Enum.Material.Plastic
			v.Reflectance = 0
		elseif v:IsA("Decal") then
			v.Transparency = 1
			v.Texture      = ""
		elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
			v.Enabled = NumberRange.new(0)
		end
	end
	for _, v in pairs(c:GetDescendants()) do
		if v:IsA("PostEffect") then
			v.Enabled = false
		end
	end
end)
local z = false
local ba  = nil
local function bb()
	if not z or not f.Character then return end
	local hrp = f.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local existing = hrp:FindFirstChild("BodyVelocity")
	if existing and existing.MaxForce == Vector3.new(100000, 0, 100000) then
		existing:Destroy()
	end
	local bv = Instance.new("BodyVelocity")
	bv.MaxForce = Vector3.new(100000, 0, 100000)
	bv.Velocity = Vector3.new(0, 0, 0)
	bv.P = 1250
	bv.Parent = hrp
end
local function bc()
	if not f.Character then return end
	local hrp = f.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local existing = hrp:FindFirstChild("BodyVelocity")
	if existing and existing.MaxForce == Vector3.new(100000, 0, 100000) then
		existing:Destroy()
	end
end
s:AddToggle("Anti Fling", function(state)
	z = state
	if state then bb() else bc() end
end)
local bd = Instance.new("Part")
bd.Name = "WalkOnWaterPart"
bd.Size = Vector3.new(4, 0.1, 4)
bd.Transparency = 1
bd.Anchored = true
bd.CanCollide = false
bd.CanTouch = false
bd.CanQuery = false
bd.Parent = workspace
local be
s:AddToggle("Walk On Water", function(state)
	if be then
		be:Disconnect()
		be = nil
	end
	bd.CanCollide = state
	if state then
		be = b.RenderStepped:Connect(function()
			local hrp = f.Character and f.Character:FindFirstChild("HumanoidRootPart")
			if hrp then
				bd.CFrame = CFrame.new(hrp.Position.X, -9.5, hrp.Position.Z)
			end
		end)
	else
		bd.CanCollide = false
		bd.Position = Vector3.new(0, -10000, 0)
	end
end)
local bf
t:AddToggle("Kill", function(state)
	_G.killBlacklistedOnly = state
	if state then
		if not bf then
			bf = b.Heartbeat:Connect(function()
				if not _G.killBlacklistedOnly then return end
				for _, plr in ipairs(a:GetPlayers()) do
					if plr ~= f and i(plr) then
						o(plr)
					end
				end
			end)
		end
	else
		if bf then
			bf:Disconnect()
			bf = nil
		end
	end
end)
local bh = {}
local function bi()
	for _, btn in ipairs(bh) do
		btn:Destroy()
	end
	table.clear(bh)
	for _, plr in ipairs(a:GetPlayers()) do
		if plr ~= f then
			local btn = t:AddButton(plr.DisplayName, function()
				if not i(plr) then
					table.insert(_G.blacklistedPlayers, plr.Name)
				end
			end, true)
			table.insert(bh, btn)
		end
	end
end
bi()
t:AddTextbox("Add To Watchlist", "Enter Clan Name", function(text)
	text = text:match("^%s*(.-)%s*$")
	if text == "" then return end
	for _, existing in ipairs(_G.watchlistedPlayers) do
		if existing:lower() == text:lower() then return end
	end
	table.insert(_G.watchlistedPlayers, text)
end)
local bg
t:AddToggle("Kill Watchlist", function(state)
	_G.killWatchlisted = state
	if state then
		if not bg then
			bg = b.Heartbeat:Connect(function()
				if not _G.killWatchlisted then return end
				for _, plr in ipairs(a:GetPlayers()) do
					if plr ~= f and k(plr) and m(plr) then
						o(plr)
					end
				end
			end)
		end
	else
		if bg then
			bg:Disconnect()
			bg = nil
		end
	end
end)
local bj = t:AddSectionLabel("Blacklist: None")
t:AddButton("Clear Blacklist", function()
	_G.blacklistedPlayers = {}
end)
local bk = t:AddSectionLabel("Watchlist: None")
t:AddButton("Clear Watchlist", function()
	_G.watchlistedPlayers = {}
end)
task.spawn(function()
	while true do
		bj.Text = #_G.blacklistedPlayers == 0 and "Blacklist: None" or "Blacklist: " .. table.concat(_G.blacklistedPlayers, ", ")
		bk.Text = #_G.watchlistedPlayers == 0 and "Watchlist: None" or "Watchlist: " .. table.concat(_G.watchlistedPlayers, ", ")
		task.wait(0.01)
	end
end)
local bl
u:AddToggle("Kill Tp", function(state)
	_G.killTpListed = state
	if state then
		if not bl then
			bl = b.Heartbeat:Connect(function()
				if not _G.killTpListed then return end
				local char = l()
				local hrp  = char and char:FindFirstChild("HumanoidRootPart")
				if not hrp then return end
				for _, name in ipairs(_G.tpListedPlayers) do
					local target = nil
					for _, plr in ipairs(a:GetPlayers()) do
						if plr.Name:lower() == name:lower() then
							target = plr
							break
						end
					end
					if target and m(target) then
						local oldCFrame = hrp.CFrame
						hrp.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 2)
						n()
						local hum = target.Character:FindFirstChildOfClass("Humanoid")
						if hum then
							hum.Died:Once(function()
								task.wait(0.01)
								if hrp and hrp.Parent then
									hrp.CFrame = oldCFrame
								end
							end)
						end
						break
					end
				end
			end)
		end
	else
		if bl then
			bl:Disconnect()
			bl = nil
		end
	end
end)
local bm = {}
local function bn()
	for _, btn in ipairs(bm) do
		btn:Destroy()
	end
	table.clear(bm)
	for _, plr in ipairs(a:GetPlayers()) do
		if plr ~= f then
			local btn = u:AddButton(plr.DisplayName, function()
				if not j(plr) then
					table.insert(_G.tpListedPlayers, plr.Name)
				end
			end, true)
			table.insert(bm, btn)
		end
	end
end
bn()
local bo = u:AddSectionLabel("TpList: None")
u:AddButton("Clear TpList", function()
	_G.tpListedPlayers = {}
end)
task.spawn(function()
	while true do
		bo.Text = #_G.tpListedPlayers == 0 and "TpList: None" or "TpList: " .. table.concat(_G.tpListedPlayers, ", ")
		task.wait(0.01)
	end
end)
local bp = nil
local bq = false
local function br()
	if f.Character then
		local hum = f.Character:FindFirstChildOfClass("Humanoid")
		if hum then
			h.CameraSubject = hum
			h.CameraType = Enum.CameraType.Custom
		end
	end
	bq = false
	bp = nil
end
local function bs(plr)
	if not plr or not plr.Character then return end
	local hum = plr.Character:FindFirstChildOfClass("Humanoid")
	if hum then
		h.CameraSubject = hum
		bp = plr
		bq = true
	end
end
local bt = {}
local function bu()
	for _, btn in ipairs(bt) do
		btn:Destroy()
	end
	table.clear(bt)
	for _, plr in ipairs(a:GetPlayers()) do
		if plr ~= f then
			local btn = v:AddButton(plr.DisplayName, function()
				if bq and bp == plr then
					br()
				else
					bs(plr)
				end
			end)
			table.insert(bt, btn)
		end
	end
end
bu()
local bv = f:WaitForChild("petsFolder"):WaitForChild("Unique")
local bw = d:WaitForChild("rEvents"):WaitForChild("equipPetEvent")
w:AddButton("Equip Wild Wizards", function()
	for _, pet in ipairs(bv:GetChildren()) do
		if pet.Name ~= "Wild Wizard" then
			bw:FireServer("unequipPet", pet)
		end
	end
	local count = 0
	for _, pet in ipairs(bv:GetChildren()) do
		if pet.Name == "Wild Wizard" and count < 8 then
			bw:FireServer("unequipPet", pet)
			bw:FireServer("equipPet", pet)
			count += 1
		end
	end
end)
w:AddButton("Equip Mighty Monsters", function()
	for _, pet in ipairs(bv:GetChildren()) do
		if pet.Name ~= "Mighty Monster" then
			bw:FireServer("unequipPet", pet)
		end
	end
	local count = 0
	for _, pet in ipairs(bv:GetChildren()) do
		if pet.Name == "Mighty Monster" and count < 8 then
			bw:FireServer("unequipPet", pet)
			bw:FireServer("equipPet", pet)
			count += 1
		end
	end
end)
local bx = false
w:AddToggle("Pack Spam WWS/MMS", function(state)
	bx = state
	if not bx then return end
	task.spawn(function()
		while bx do
			for _, pet in ipairs(bv:GetChildren()) do
				if not bx then break end
				if pet.Name == "Mighty Monster" then
					bw:FireServer("unequipPet", pet)
				end
			end
			local count = 0
			for _, pet in ipairs(bv:GetChildren()) do
				if not bx then break end
				if pet.Name == "Wild Wizard" and count < 8 then
					bw:FireServer("unequipPet", pet)
					bw:FireServer("equipPet", pet)
					count += 1
				end
			end
			task.wait(0.15)
			if not bx then break end
			for _, pet in ipairs(bv:GetChildren()) do
				if not bx then break end
				if pet.Name == "Wild Wizard" then
					bw:FireServer("unequipPet", pet)
				end
			end
			count = 0
			for _, pet in ipairs(bv:GetChildren()) do
				if not bx then break end
				if pet.Name == "Mighty Monster" and count < 8 then
					bw:FireServer("unequipPet", pet)
					bw:FireServer("equipPet", pet)
					count += 1
				end
			end
			task.wait(0.15)
		end
	end)
end)
local by = false
w:AddToggle("Pack Spam MM", function(state)
	by = state
	if not by then return end
	task.spawn(function()
		while by do
			local count = 0
			for _, pet in ipairs(bv:GetChildren()) do
				if not by then break end
				if pet.Name == "Mighty Monster" and count < 8 then
					bw:FireServer("unequipPet", pet)
					bw:FireServer("equipPet", pet)
					count += 1
				end
			end
			task.wait(0.15)
		end
	end)
end)
local bz = nil
local ca = {}
local function cb()
	for _, btn in ipairs(ca) do
		btn:Destroy()
	end
	table.clear(ca)
	for _, plr in ipairs(a:GetPlayers()) do
		local btn = x:AddButton(plr.DisplayName, function()
			bz = plr
		end, true)
		table.insert(ca, btn)
	end
end
cb()
local cc = x:AddSectionLabel("Name: N/A")
local cd = x:AddSectionLabel("Strength: N/A")
local ce = x:AddSectionLabel("Durability: N/A")
local function cf(plr)
	if not plr then return end
	cc.Text = "Name: " .. plr.DisplayName
	if not plr:FindFirstChild("leaderstats") then return end
	local function setStat(label, name, key)
		local val = plr.leaderstats:FindFirstChild(key) or plr:FindFirstChild(key)
		if val then
			label.Text = string.format("%s: %s (%s)", name, p(val.Value), q(val.Value))
		else
			label.Text = name .. ": 0 (0)"
		end
	end
	setStat(cd, "Strength", "Strength")
	setStat(ce, "Durability", "Durability")
end
task.spawn(function()
	while true do
		if bz then cf(bz) end
		task.wait(0.01)
	end
end)
local cg = x:AddSectionLabel("Enemy Health: N/A")
local ch = x:AddSectionLabel("Enemy Damage: N/A")
local ci = x:AddSectionLabel("Your Health: N/A")
local cj = x:AddSectionLabel("Your Damage: N/A")
local ck = x:AddSectionLabel("Hits To Kill: N/A")
local function cl(plr)
	if not plr then return 0 end
	local dur = plr:FindFirstChild("Durability") or (plr:FindFirstChild("leaderstats") and plr.leaderstats:FindFirstChild("Durability"))
	if not dur then return 0 end
	local mult = 1
	if plr:FindFirstChild("ultimatesFolder") and plr.ultimatesFolder:FindFirstChild("Infernal Health") then
		mult = mult + 0.15 * (plr.ultimatesFolder["Infernal Health"].Value or 0)
	end
	if plr:FindFirstChild("equippedPets") then
		for _, pet in ipairs(plr.equippedPets:GetChildren()) do
			if pet:IsA("ObjectValue") and pet.Value then
				local n = string.lower(pet.Value.Name)
				if n:match("mighty") and n:match("monster") then mult = mult + 0.5 end
				if n:match("small") and n:match("fry") then mult = mult + 0.25 end
			end
		end
	end
	return dur.Value * mult
end
local function cm(plr)
	if not plr or not plr:FindFirstChild("leaderstats") or not plr.leaderstats:FindFirstChild("Strength") then return 0 end
	local base = plr.leaderstats.Strength.Value * (1/15)
	local mult = 1
	if plr:FindFirstChild("ultimatesFolder") and plr.ultimatesFolder:FindFirstChild("Demon Damage") then
		mult = mult + 0.1 * (plr.ultimatesFolder["Demon Damage"].Value or 0)
	end
	if plr:FindFirstChild("equippedPets") then
		for _, pet in ipairs(plr.equippedPets:GetChildren()) do
			if pet:IsA("ObjectValue") and pet.Value then
				local n = string.lower(pet.Value.Name)
				if n:match("wild") and n:match("wizard") then mult = mult + 0.5 end
				if n:match("chaos") and n:match("sorcerer") then mult = mult + 0.25 end
			end
		end
	end
	return base * mult
end
local function cn(plr)
	if not plr then
		cg.Text = "Enemy Health: N/A"
		ch.Text = "Enemy Damage: N/A"
		ci.Text = "Your Health: N/A"
		cj.Text = "Your Damage: N/A"
		ck.Text = "Hits To Kill: N/A"
		return
	end
	local eHealth = cl(plr)
	local eDamage = cm(plr)
	local yHealth = cl(f)
	local yDamage = cm(f)
	local hits = "∞"
	if yDamage > 0 then
		local h = math.ceil(eHealth / yDamage)
		if h > 200 then hits = "∞"
		elseif h < 1 then hits = "instant"
		else hits = tostring(h) end
	end
	cg.Text = string.format("Enemy Health: %s (%s)", p(eHealth), q(eHealth))
	ch.Text = string.format("Enemy Damage: %s (%s)", p(eDamage), q(eDamage))
	ci.Text = string.format("Your Health: %s (%s)", p(yHealth), q(yHealth))
	cj.Text = string.format("Your Damage: %s (%s)", p(yDamage), q(yDamage))
	ck.Text = "Hits To Kill: " .. hits
end
task.spawn(function()
	while true do
		cn(bz)
		task.wait(0.01)
	end
end)
f.CharacterAdded:Connect(function(char)
	char:WaitForChild("HumanoidRootPart", 5)
	ba = char:FindFirstChildOfClass("Humanoid")
	if z then bb() end
	local hum = char:WaitForChild("Humanoid", 3)
	if hum then ba = hum end
	task.wait(0.05)
	if bq and bp then
		bs(bp)
	else
		if ba then h.CameraSubject = ba end
	end
end)
if f.Character then
	ba = f.Character:FindFirstChildOfClass("Humanoid")
end
a.PlayerAdded:Connect(function()
	task.wait(0.01)
	bi()
	bn()
	bu()
	cb()
end)
a.PlayerRemoving:Connect(function(plr)
	task.wait(0.01)
	if bp == plr then
		br()
	end
	bi()
	bn()
	bu()
	cb()
end)
